{
    "name": "app-control-ganadero-root",
    "private": true,
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "scripts": {
        "dev": "npm run dev --workspace=soto-livestock-app",
        "build": "npm run build --workspace=soto-livestock-app",
        "start": "npm run start --workspace=soto-livestock-app"
    }
}